function message(el) {
    /**
    * Logs the innerHTML of an element to the console.
    * 
    * @param {HTMLElement} el - The element to log the innerHTML of.
    * @returns {void}
    */
    console.dir(el.innerHTML);
}